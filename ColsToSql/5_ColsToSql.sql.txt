CREATE TABLE Individus (
AAC INTEGER,
ACPER INTEGER,
ACTEU INTEGER,
ACTEU6 INTEGER,
ACTOP INTEGER,
ADFDAP INTEGER,
AG INTEGER,
AG3 INTEGER,
AG3B INTEGER,
AG5 INTEGER,
AGE INTEGER,
AGE3 INTEGER,
AGE3B INTEGER,
AGE5 INTEGER,
AGEQ INTEGER,
AGQ INTEGER,
AIDMEN INTEGER,
ALCNC INTEGER,
ALCT INTEGER,
AM1NB INTEGER,
AM2NB INTEGER,
AMOIS INTEGER,
ANBSAL INTEGER,
ANCCHOM INTEGER,
ANCCHOMM INTEGER,
ANCENTR INTEGER,
ANCENTR4 INTEGER,
ANCINACT INTEGER,
ANCINATM INTEGER,
ANNEE INTEGER,
ART INTEGER,
ASSOCI INTEGER,
AUTSAL INTEGER,
CA INTEGER,
CAPITA INTEGER,
CHGM INTEGER,
CHPUB INTEGER,
CIRC INTEGER,
CLIEN VARCHAR(1),
COMSAL INTEGER,
CONTRA INTEGER,
CREANT INTEGER,
CSA INTEGER,
CSAR INTEGER,
CSE INTEGER,
CSEI INTEGER,
CSEP INTEGER,
CSEPR INTEGER,
CSER INTEGER,
CSPIP INTEGER,
CSPM INTEGER,
CSPP INTEGER,
CSS2 INTEGER,
CSS3 INTEGER,
CSS4 VARCHAR(1),
CSTMN INTEGER,
CSTOT INTEGER,
CSTOTI INTEGER,
CSTOTR INTEGER,
CSTPLC INTEGER,
DAMOIS INTEGER,
DATANT INTEGER,
DATDEB INTEGER,
DDIPL INTEGER,
DEJTRA INTEGER,
DEM INTEGER,
DEP INTEGER,
DIMANC INTEGER,
DIMHEU INTEGER,
DIMTYP INTEGER,
DIP INTEGER,
DIP11 INTEGER,
DIPP INTEGER,
DISPOC INTEGER,
DISPPLC INTEGER,
DNAI VARCHAR(2),
DREMC INTEGER,
DREMCM INTEGER,
DUDET INTEGER,
DUHAB INTEGER,
DURSTG INTEGER,
ECHPUB INTEGER,
ECONTR INTEGER,
ECREC INTEGER,
EDEP INTEGER,
EMPABS INTEGER,
EMPAFC VARCHAR(3),
EMPAFG VARCHAR(2),
EMPAFP VARCHAR(2),
EMPANH VARCHAR(3),
EMPCE VARCHAR(2),
EMPCO VARCHAR(3),
EMPCON INTEGER,
EMPCP VARCHAR(1),
EMPHNH DECIMAL(1.1),
EMPHRC DECIMAL(1.1),
EMPHRE DECIMAL(1.1),
EMPHSC INTEGER,
EMPJF VARCHAR(2),
EMPNBH DECIMAL(2.2),
EMPPA VARCHAR(2),
EOCCUA INTEGER,
ERESID INTEGER,
ESSAI INTEGER,
ESTC INTEGER,
ETITC INTEGER,
ETMC INTEGER,
ETPP INTEGER,
EXPLAG INTEGER,
FAMIL INTEGER,
FC5B INTEGER,
FC5D INTEGER,
FONCTC INTEGER,
FORDAT INTEGER,
FORM INTEGER,
FORTER INTEGER,
FORTYP INTEGER,
FRANCE INTEGER,
HHC DECIMAL(2.2),
HHC6 INTEGER,
HORAIC INTEGER,
IDENT VARCHAR(7),
IMMI INTEGER,
JOURTR DECIMAL(1.2),
LLURPA INTEGER,
LNAIS INTEGER,
LWORHA INTEGER,
MAAHE INTEGER,
MAISOC INTEGER,
MATRI INTEGER,
MAVE VARCHAR(1),
MENPAR INTEGER,
MENSAL INTEGER,
MMENT INTEGER,
MMVIE INTEGER,
MRA INTEGER,
MRB INTEGER,
MRC INTEGER,
MRD INTEGER,
MRE INTEGER,
MREC INTEGER,
MRF INTEGER,
MRG INTEGER,
MRH INTEGER,
MRI INTEGER,
MRJ INTEGER,
MRK INTEGER,
MRL INTEGER,
MRM INTEGER,
MRN INTEGER,
MRO INTEGER,
MVL INTEGER,
NAIA INTEGER,
NAIM INTEGER,
NATEU15 INTEGER,
NBCHOME INTEGER,
NBINACT INTEGER,
NBSALA INTEGER,
NBSALB INTEGER,
NBTEMP INTEGER,
NIVET INTEGER,
NIVP INTEGER,
NOI INTEGER,
NOICON INTEGER,
NOIENF10 VARCHAR(1),
NOIENF11 VARCHAR(1),
NOIENF12 VARCHAR(1),
NOIENF13 VARCHAR(1),
NOIENF14 VARCHAR(1),
NOIMER INTEGER,
NOIPER INTEGER,
NONDIC INTEGER,
NREC INTEGER,
NRESID INTEGER,
NUITC INTEGER,
OCCREF INTEGER,
OFFICC INTEGER,
P VARCHAR(4),
PASTRA INTEGER,
PASTRB INTEGER,
PASTRF INTEGER,
PAV VARCHAR(4),
PAYNEU15 INTEGER,
PENT VARCHAR(4),
PRESAL INTEGER,
PRIM INTEGER,
PRIMS INTEGER,
QPRC INTEGER,
RABS INTEGER,
RABSP INTEGER,
RAISTP INTEGER,
RCHG INTEGER,
RDET INTEGER,
REA INTEGER,
REG INTEGER,
RES INTEGER,
REVENT VARCHAR(1),
RGA INTEGER,
RGI INTEGER,
RGL INTEGER,
RSTG INTEGER,
SALMEE INTEGER,
SALMET VARCHAR(1),
SALPAR INTEGER,
SALRED INTEGER,
SALREDTR VARCHAR(1),
SALSEE INTEGER,
SERDOM INTEGER,
SEXE INTEGER,
SITANT INTEGER,
SO INTEGER,
SOIRC INTEGER,
SOU INTEGER,
SOUA INTEGER,
SOUB INTEGER,
SOUSEMPL INTEGER,
SP00 INTEGER,
SP01 INTEGER,
SP02 INTEGER,
SP03 INTEGER,
SP04 INTEGER,
SP05 INTEGER,
SP06 INTEGER,
SP07 INTEGER,
SP08 INTEGER,
SP09 INTEGER,
SP10 INTEGER,
SP11 INTEGER,
SPE INTEGER,
SPEFC INTEGER,
SPEP INTEGER,
STAT2 INTEGER,
STAT2P INTEGER,
STATOEP INTEGER,
STATUT INTEGER,
STATUTP INTEGER,
STATUTR INTEGER,
STATUTS2 INTEGER,
STATUTS3 INTEGER,
STATUTS4 VARCHAR(1),
STC INTEGER,
STCHE INTEGER,
STJC INTEGER,
STMN INTEGER,
STNRED INTEGER,
STNREDP INTEGER,
STPLC INTEGER,
TAM1B INTEGER,
TAM1C INTEGER,
TAM1D INTEGER,
TEMP INTEGER,
TITC INTEGER,
TPP INTEGER,
TPPRED INTEGER,
TPSINT INTEGER,
TRAREF INTEGER,
TRIM INTEGER,
TTRREF INTEGER,
TYPC INTEGER,
TYPSAL INTEGER,
TYPSEC INTEGER,
ULTJ INTEGER,
VALPRE INTEGER,
VALPRIE INTEGER,
ZUS INTEGER
, PRIMARY KEY (ANNEE, TRIM, IDENT, NOI)
)
